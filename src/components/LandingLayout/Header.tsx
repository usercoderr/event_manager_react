import {FC} from "react";
import Navbar from "../ui/Navbar.tsx";

const Header:FC = () =>{
    return(
        <header className={'header'}>
            <Navbar/>
        </header>
    )
}
export default Header
